<UserControl x:Class="RadialActions.ActionEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:RadialActions"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance Type=local:ActionEditorViewModel}"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/RadialActions;component/Settings/SettingsViewResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <StackPanel>
        <!-- Identity: the icon and name form one visual unit, like the slice they produce. -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Style="{StaticResource ActionEditorLabelTextBlock}"
                           Text="Icon" />
                <TextBox Width="72"
                         FontSize="18"
                         TextAlignment="Center"
                         Text="{Binding SelectedAction.Icon, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Style="{StaticResource ActionEditorLabelTextBlock}"
                           Text="Name" />
                <TextBox Text="{Binding SelectedAction.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
        </Grid>
        <TextBlock Style="{StaticResource DescriptionTextBlock}"
                   Text="Shown together in the slice. Emoji work well as icons." />

        <TextBlock Style="{StaticResource ActionEditorLabelTextBlock}"
                   Text="Type" />
        <ComboBox ItemsSource="{Binding ActionTypes}"
                  SelectedValue="{Binding SelectedActionType, Mode=TwoWay}"
                  SelectedValuePath="Type">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Width="20"
                                   FontSize="14"
                                   Text="{Binding Icon}" />
                        <TextBlock Margin="4,0,0,0"
                                   VerticalAlignment="Center"
                                   Text="{Binding Name}" />
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <TextBlock Style="{StaticResource DescriptionTextBlock}"
                   Text="Key runs shortcuts and media controls. Shell opens apps, files, folders, URLs, or commands." />

        <StackPanel Visibility="{Binding SelectedActionType, Converter={local:MatchToVisibilityConverter}, ConverterParameter={x:Static local:ActionType.Key}}">
            <TextBlock Style="{StaticResource ActionEditorLabelTextBlock}"
                       Text="Key action" />
            <ComboBox IsTextSearchEnabled="True"
                      ItemsSource="{Binding KeyActionOptionsView}"
                      MaxDropDownHeight="420"
                      SelectedValue="{Binding SelectedKeyActionId, Mode=TwoWay}"
                      SelectedValuePath="Id"
                      TextSearch.TextPath="Name">
                <ComboBox.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate>
                                <TextBlock Margin="6,6,0,2"
                                           FontSize="11"
                                           FontWeight="SemiBold"
                                           Opacity="0.6"
                                           Text="{Binding Name}" />
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                    </GroupStyle>
                </ComboBox.GroupStyle>
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Width="20"
                                       FontSize="14"
                                       Text="{Binding Icon}" />
                            <TextBlock Margin="4,0,0,0"
                                       VerticalAlignment="Center"
                                       Text="{Binding Name}" />
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <TextBlock Style="{StaticResource DescriptionTextBlock}"
                       Text="Pick a media, volume, or system command, or record your own with Custom Shortcut." />

            <StackPanel Visibility="{Binding SelectedKeyActionId, Converter={local:MatchToVisibilityConverter}, ConverterParameter={x:Static local:ActionEditorViewModel.CustomKeyActionId}}">
                <TextBlock Style="{StaticResource ActionEditorLabelTextBlock}"
                           Text="Shortcut" />
                <TextBox Tag="HotkeyInput"
                         Text="{Binding SelectedAction.Parameter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Style="{StaticResource DescriptionTextBlock}"
                           Text="Click the box, then press the key combination such as Ctrl+Shift+M. Press Backspace to clear." />
            </StackPanel>
        </StackPanel>

        <StackPanel Visibility="{Binding SelectedActionType, Converter={local:MatchToVisibilityConverter}, ConverterParameter={x:Static local:ActionType.Shell}}">
            <TextBlock Style="{StaticResource ActionEditorLabelTextBlock}"
                       Text="Target" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="6" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0"
                         Text="{Binding SelectedAction.Parameter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Column="2"
                        Padding="8,2"
                        Command="{Binding BrowseShellTargetCommand}"
                        Content="Browse..." />
            </Grid>
            <TextBlock Style="{StaticResource DescriptionTextBlock}"
                       Text="The app, file, folder, URL, or command to open." />

            <TextBlock Style="{StaticResource ActionEditorLabelTextBlock}"
                       Text="Arguments" />
            <TextBox Text="{Binding SelectedAction.Arguments, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Style="{StaticResource DescriptionTextBlock}"
                       Text="Optional command-line arguments for the target." />

            <TextBlock Style="{StaticResource ActionEditorLabelTextBlock}"
                       Text="Working directory" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="6" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0"
                         Text="{Binding SelectedAction.WorkingDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Column="2"
                        Padding="8,2"
                        Command="{Binding BrowseWorkingDirectoryCommand}"
                        Content="Browse..." />
            </Grid>
            <TextBlock Style="{StaticResource DescriptionTextBlock}"
                       Text="Optional folder to use as the target's start location." />
        </StackPanel>

        <Separator Margin="0,4,0,10"
                   Opacity="0.35" />
        <CheckBox Content="Show this action in the menu"
                  IsChecked="{Binding SelectedAction.IsEnabled, Mode=TwoWay}"
                  ToolTip="Hide the action without removing its configuration." />
    </StackPanel>
</UserControl>
