<Window x:Class="RadialActions.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RadialActions"
        xmlns:p="clr-namespace:RadialActions.Properties"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        d:DataContext="{d:DesignInstance Type=local:MainWindow}"
        mc:Ignorable="d"
        Title="{x:Static local:App.FriendlyName}"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Opacity="0"
        ShowInTaskbar="False"
        Topmost="True"
        Loaded="Window_Loaded"
        Unloaded="Window_Unloaded"
        Closing="Window_Closing"
        Deactivated="Window_Deactivated"
        KeyDown="Window_KeyDown">
    <Window.Resources>
        <ContextMenu x:Key="MainContextMenu"
                     x:Shared="False">

            <MenuItem Command="{Binding OpenSettingsWindowCommand}"
                      CommandParameter="0"
                      Header="_Settings">
                <MenuItem.Icon>
                    <TextBlock FontFamily="Segoe MDL2 Assets"
                               Text="&#xE713;" />
                </MenuItem.Icon>
            </MenuItem>

            <MenuItem Command="{Binding OpenSettingsWindowCommand}"
                      CommandParameter="1"
                      Header="_Configure actions">
                <MenuItem.Icon>
                    <TextBlock FontFamily="Segoe MDL2 Assets"
                               Text="&#xE7FC;" />
                </MenuItem.Icon>
            </MenuItem>

            <MenuItem Command="{Binding OpenSettingsWindowCommand}"
                      CommandParameter="2"
                      Header="_Help and shortcuts">
                <MenuItem.Icon>
                    <TextBlock FontFamily="Segoe MDL2 Assets"
                               Text="&#xE897;" />
                </MenuItem.Icon>
            </MenuItem>

            <Separator />

            <MenuItem Command="{Binding OpenSettingsFileCommand}"
                      Header="Ad_vanced settings file"
                      IsEnabled="{x:Static p:Settings.CanBeSaved}">
                <MenuItem.Icon>
                    <TextBlock FontFamily="Segoe MDL2 Assets"
                               Text="&#xE8A5;" />
                </MenuItem.Icon>
            </MenuItem>

            <MenuItem Command="{Binding ExitCommand}"
                      Header="E_xit">
                <MenuItem.Icon>
                    <TextBlock FontFamily="Segoe MDL2 Assets"
                               Text="&#xE8BB;" />
                </MenuItem.Icon>
            </MenuItem>
        </ContextMenu>

        <tb:TaskbarIcon x:Key="TrayIcon"
                        x:Shared="False"
                        IconSource="RadialActions.ico"
                        ToolTipText="Radial Actions&#10;Left click: open menu&#10;Right click: options"
                        MenuActivation="RightClick"
                        TrayLeftMouseDown="OnTrayLeftMouseDown" />
    </Window.Resources>

    <Window.Style>
        <Style TargetType="Window">
            <Style.Triggers>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Visibility}"
                             Value="Visible">
                    <DataTrigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 From="0"
                                                 To="1"
                                                 Duration="0:0:0.08" />
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Style>

    <Grid>
        <local:PieControl Slices="{Binding Actions, Source={x:Static p:Settings.Default}}"
                          Width="{Binding Size, Source={x:Static p:Settings.Default}}"
                          Height="{Binding Size, Source={x:Static p:Settings.Default}}"
                          SliceClicked="OnSliceClicked" />
    </Grid>
</Window>
